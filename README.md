## Getting started

To get started with the app, clone the repo and then install the needed gems:

``` $ bundle install ```

``` $ rails db:migrate:reset ```

`$ rails db:seed`

``` $ rails server ```

