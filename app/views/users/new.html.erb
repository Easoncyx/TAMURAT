<% if company_representative? %>
  <% provide(:title, 'Invite') %>
  <% provide(:button_text, 'Invite') %>
  <h1> Invite Subcompany</h1>
<% else %>
  <% provide(:title, 'Sign up') %>
  <% provide(:button_text, 'Create new account') %>
  <h1>Sign up</h1>
<% end %>


<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
</div>
